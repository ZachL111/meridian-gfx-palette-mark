# Review Journal

I treated `meridian-gfx-palette-mark` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its graphics focus without claiming live deployment or external usage.

## Cases

- `baseline`: `geometry span`, score 194, lane `ship`
- `stress`: `atlas pressure`, score 168, lane `ship`
- `edge`: `shader drift`, score 245, lane `ship`
- `recovery`: `render budget`, score 226, lane `ship`
- `stale`: `geometry span`, score 188, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
